import { swapi_url } from "../SWAPI"

function About() {return(
    <div id="about">
        Welp, this is about <code>S<sub>tar</sub>W<sub>ars</sub>API</code> and how to fetch
        stuff with it from <code>{swapi_url}</code>. Have fun!
    </div>
)}

export default About
